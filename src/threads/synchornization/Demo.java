package threads.synchornization;

public class Demo {
    /*we have to achieve mutual exclusion
    In Java, each object automatically contains a hidden lock inside it.

    This lock is called:
    Monitor lock
    Intrinsic lock
    Object lock

    these locks are used to enter the critical section*/
// it locks obj or class if the elements are static -- only locks synchronized blocks or methods not normal methods and variables
}
