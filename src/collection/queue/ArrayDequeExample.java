package collection.queue;

import java.util.ArrayDeque;

public class ArrayDequeExample {

    /*
        same functions as LinkedList Queue but internally it uses Circular Queue
        uses Circular queue

        Similar to LinedList but it have circular queue which is quite faster compared to getting elements of linked list
            --beucase it is shattered we have to look on memory reference
        cant have null
    */

    public static void run(){

        ArrayDeque<Integer> queue = new ArrayDeque<>();

    }
}
