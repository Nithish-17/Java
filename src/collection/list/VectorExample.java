package collection.list;

/*
    1. same as ArrayList but Thread safe -- all methods are synchronized--------
    2. --- have one extra constructor that allows to change the increment size--------
            Vector<Integer> vector = new Vector<>(10, 5);

    3. Performance is slightly slower compared to ArrayList because it does have locking overhead
*/

public class VectorExample {

}
